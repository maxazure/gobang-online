# 后端环境变量配置

# 服务器端口
PORT=3001

# 前端 URL（用于 CORS）
CLIENT_URL=http://localhost:5173

# 数据库配置（PostgreSQL）
DATABASE_URL=postgresql://user:password@localhost:5432/gobang

# Redis 配置
REDIS_URL=redis://localhost:6379

# JWT 密钥
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# 环境
NODE_ENV=development
